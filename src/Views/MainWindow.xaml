<Window x:Class="LineManagementSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="برنامج احمد السيسي لادارة الخطوط" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft"
        Background="{DynamicResource WindowBackground}">

    <Window.Resources>
        <Style x:Key="ProviderButton" TargetType="Button">
            <Setter Property="Width" Value="280"/>
            <Setter Property="Height" Value="280"/>
            <Setter Property="Margin" Value="25"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{DynamicResource CardBackground}"
                                CornerRadius="24"
                                BorderThickness="2"
                                BorderBrush="{DynamicResource BorderColor}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="20" ShadowDepth="5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Opacity="0.25" BlurRadius="30" ShadowDepth="8"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AlertCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8,5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="8" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="15" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" 
                Style="{StaticResource ModernCard}"
                Margin="10,10,10,15">
            <StackPanel>
                <DockPanel Margin="0,0,0,20">
                    <Button Content="🔍 بحث شامل" 
                            DockPanel.Dock="Left"
                            Click="SearchButton_Click"
                            Padding="25,15"
                            Margin="0,0,20,0"
                            Background="#2196F3"
                            Foreground="White"
                            FontSize="18"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="12"
                                        Padding="{TemplateBinding Padding}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="3"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Content="⚙️ الإعدادات" 
                            DockPanel.Dock="Right"
                            Click="SettingsButton_Click"
                            Padding="25,15"
                            Margin="20,0,0,0"
                            Background="#607D8B"
                            Foreground="White"
                            FontSize="18"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="12"
                                        Padding="{TemplateBinding Padding}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="3"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="لوف يو فيري ماتش يا مصر" 
                                  FontSize="32" 
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource TextPrimary}"
                                  HorizontalAlignment="Center"
                                  Margin="0,0,0,8"/>
                        <TextBlock Text="احمد السيسي" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource TextSecondary}"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </DockPanel>

                <!-- Alerts Section -->
                <StackPanel Margin="0,10,0,0" x:Name="AlertsPanel">
                    <DockPanel Margin="0,0,0,15">
                        <TextBlock Text="🔔 التنبيهات" 
                                  FontSize="22" 
                                  FontWeight="Bold" 
                                  Foreground="#E60000"
                                  VerticalAlignment="Center"/>
                        <Button Content="مسح الكل" 
                                DockPanel.Dock="Left"
                                Command="{Binding ClearAlertsCommand}"
                                Padding="15,8"
                                Margin="10,0,0,0"
                                Background="#F44336"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DockPanel>

                    <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="AlertsList" ItemsSource="{Binding Alerts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource AlertCard}" Background="{Binding GetColorBrush}">
                                        <DockPanel>
                                            <Border DockPanel.Dock="Right" 
                                                    Background="White" 
                                                    CornerRadius="25" 
                                                    Width="45" 
                                                    Height="45" 
                                                    Margin="0,0,20,0"
                                                    VerticalAlignment="Center">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="5" ShadowDepth="2"/>
                                                </Border.Effect>
                                                <TextBlock Text="⚠" 
                                                          FontSize="26" 
                                                          HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"
                                                          Foreground="#FF6B00"/>
                                            </Border>
                                            
                                            <Button Content="✓ إخفاء" 
                                                    DockPanel.Dock="Left"
                                                    Command="{Binding DataContext.MarkAlertReadCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding Id}"
                                                    Padding="15,8"
                                                    Background="White"
                                                    Foreground="#4CAF50"
                                                    BorderThickness="0"
                                                    FontWeight="Bold"
                                                    FontSize="14"
                                                    Cursor="Hand"
                                                    Margin="15,0,0,0"
                                                    VerticalAlignment="Center">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="8" 
                                                                Padding="{TemplateBinding Padding}">
                                                            <Border.Effect>
                                                                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="5" ShadowDepth="2"/>
                                                            </Border.Effect>
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Opacity" Value="0.85"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            
                                            <TextBlock Text="{Binding GetArabicMessage}" 
                                                      FontSize="16"
                                                      FontWeight="SemiBold"
                                                      Foreground="White"
                                                      TextWrapping="Wrap"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,20,0"/>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Providers Section -->
        <Grid Grid.Row="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="اختر الشركة" 
                          FontSize="28" 
                          FontWeight="Bold"
                          Foreground="{DynamicResource TextSecondary}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,40"/>

                <WrapPanel HorizontalAlignment="Center">
                    <Button Style="{StaticResource ProviderButton}" 
                            Click="VodafoneButton_Click"
                            ToolTip="فودافون - اضغط للإدارة">
                        <StackPanel>
                            <Image Source="/Resources/Images/vodafone.png" 
                                   Width="200" 
                                   Height="200"
                                   Stretch="Uniform"
                                   Margin="0,10,0,15"/>
                            <TextBlock Text="Vodafone" 
                                      FontSize="20" 
                                      FontWeight="Bold" 
                                      Foreground="#E60000"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ProviderButton}" 
                            Click="EtisalatButton_Click"
                            ToolTip="اتصالات - اضغط للإدارة">
                        <StackPanel>
                            <Image Source="/Resources/Images/etisalat.png" 
                                   Width="200" 
                                   Height="200"
                                   Stretch="Uniform"
                                   Margin="0,10,0,15"/>
                            <TextBlock Text="Etisalat" 
                                      FontSize="20" 
                                      FontWeight="Bold" 
                                      Foreground="#7FBA00"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ProviderButton}" 
                            Click="WeButton_Click"
                            ToolTip="وي - اضغط للإدارة">
                        <StackPanel>
                            <Image Source="/Resources/Images/we.png" 
                                   Width="200" 
                                   Height="200"
                                   Stretch="Uniform"
                                   Margin="0,10,0,15"/>
                            <TextBlock Text="WE" 
                                      FontSize="20" 
                                      FontWeight="Bold" 
                                      Foreground="#6A1B9A"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ProviderButton}" 
                            Click="OrangeButton_Click"
                            ToolTip="اورنج - اضغط للإدارة">
                        <StackPanel>
                            <Image Source="/Resources/Images/orange.png" 
                                   Width="200" 
                                   Height="200"
                                   Stretch="Uniform"
                                   Margin="0,10,0,15"/>
                            <TextBlock Text="Orange" 
                                      FontSize="20" 
                                      FontWeight="Bold" 
                                      Foreground="#FF7900"
                                      HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
