<Window x:Class="LineManagementSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="نظام إدارة خطوط الهاتف المحمول" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style x:Key="ProviderButton" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="15" ShadowDepth="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AlertCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="5" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="5" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel>
                <DockPanel>
                    <Button Content="🔍 بحث شامل" 
                            DockPanel.Dock="Left"
                            Click="SearchButton_Click"
                            Padding="20,10"
                            Margin="0,0,20,0"
                            Background="#2196F3"
                            Foreground="White"
                            FontSize="16"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                        </Button.Effect>
                    </Button>
                    <TextBlock Text="نظام إدارة خطوط الهاتف المحمول" 
                              FontSize="28" 
                              FontWeight="Bold"
                              Foreground="#333"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </DockPanel>
                
                <StackPanel Margin="0,20,0,0" x:Name="AlertsPanel">
                    <DockPanel>
                        <TextBlock Text="التنبيهات" FontSize="20" FontWeight="Bold" Foreground="#E60000"/>
                        <Button Content="مسح الكل" 
                                DockPanel.Dock="Left"
                                Command="{Binding ClearAlertsCommand}"
                                Padding="10,5"
                                Margin="10,0,0,0"
                                Background="#F44336"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"/>
                    </DockPanel>
                    
                    <ScrollViewer MaxHeight="200" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="AlertsList" ItemsSource="{Binding Alerts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource AlertCard}" Background="{Binding GetColorBrush}">
                                        <DockPanel>
                                            <Button Content="✓" 
                                                    DockPanel.Dock="Left"
                                                    Command="{Binding DataContext.MarkAlertReadCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding Id}"
                                                    Width="30"
                                                    Height="30"
                                                    Background="White"
                                                    Foreground="Green"
                                                    BorderThickness="0"
                                                    FontWeight="Bold"
                                                    Cursor="Hand"
                                                    Margin="10,0,0,0"/>
                                            <TextBlock Text="{Binding GetArabicMessage}" 
                                                      FontSize="14"
                                                      Foreground="White"
                                                      TextWrapping="Wrap"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0"/>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="اختر الشركة" 
                          FontSize="24" 
                          FontWeight="Bold"
                          Foreground="#666"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,30"/>
                
                <WrapPanel HorizontalAlignment="Center">
                    <Button Style="{StaticResource ProviderButton}" 
                            Background="White"
                            Click="VodafoneButton_Click">
                        <Image Source="/src/Resources/Images/vodafone.png" 
                               Width="180" 
                               Height="180"
                               Stretch="Uniform"/>
                    </Button>

                    <Button Style="{StaticResource ProviderButton}" 
                            Background="White"
                            Click="EtisalatButton_Click">
                        <Image Source="/src/Resources/Images/etisalat.png" 
                               Width="180" 
                               Height="180"
                               Stretch="Uniform"/>
                    </Button>

                    <Button Style="{StaticResource ProviderButton}" 
                            Background="White"
                            Click="WeButton_Click">
                        <Image Source="/src/Resources/Images/we.png" 
                               Width="180" 
                               Height="180"
                               Stretch="Uniform"/>
                    </Button>

                    <Button Style="{StaticResource ProviderButton}" 
                            Background="White"
                            Click="OrangeButton_Click">
                        <Image Source="/src/Resources/Images/orange.png" 
                               Width="180" 
                               Height="180"
                               Stretch="Uniform"/>
                    </Button>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
