<Window x:Class="LineManagementSystem.Views.GroupDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LineManagementSystem.Converters"
        mc:Ignorable="d"
        Title="{Binding Group.Name, StringFormat='تفاصيل المجموعة: {0}'}"
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft"
        Background="{DynamicResource WindowBackground}"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:ConfirmationLevelToColorConverter x:Key="ConfirmLevelToColor"/>
        <local:ConfirmationLevel1VisibilityConverter x:Key="ConfirmLevel1Vis"/>
        <local:ConfirmationLevel2VisibilityConverter x:Key="ConfirmLevel2Vis"/>
        <local:ConfirmationLevel3VisibilityConverter x:Key="ConfirmLevel3Vis"/>
        <local:ProviderToColorConverter x:Key="ProviderToColorConverter"/>
        <local:PhoneLineToColorConverter x:Key="PhoneLineToColorConverter"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{Binding Group.Provider, Converter={StaticResource ProviderToColorConverter}}"
                Padding="20">
            <StackPanel>
                <TextBlock Text="{Binding Group.Name, StringFormat='المجموعة: {0}'}" 
                           FontSize="32" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="{Binding Group.GetLineCount, StringFormat='عدد الخطوط: {0}'}" 
                           FontSize="18" Margin="0,5,0,0" Foreground="White" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="20,20,20,20" Background="{DynamicResource CardBackground}" Padding="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="5" ShadowDepth="2"/>
            </Border.Effect>
            <WrapPanel Orientation="Horizontal">
                <Button Content="+ إضافة خط جديد"
                        Style="{StaticResource ActionButton}"
                        Background="#43A047"
                        Command="{Binding AddLineCommand}">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
                <Button Content="✏ تعديل الخط"
                        Style="{StaticResource ActionButton}"
                        Background="#1E88E5"
                        Command="{Binding EditLineCommand}">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
                <Button Content="🗑 حذف الخط"
                        Style="{StaticResource ActionButton}"
                        Background="#E53935"
                        Command="{Binding DeleteLineCommand}">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
                <Button Content="💰 تحديد الكل بمحفظة"
                        Style="{StaticResource ActionButton}"
                        Background="#FF9800"
                        Command="{Binding SetAllLinesWithWalletCommand}">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
                <Button Content="📥 استيراد من Excel"
                        Style="{StaticResource ActionButton}"
                        Background="#9C27B0"
                        Click="ImportExcel_Click">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
                <Button Content="📄 تصدير Excel"
                        Style="{StaticResource ActionButton}"
                        Background="#43A047"
                        Click="ExportExcel_Click">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
                <Button Content="📑 تصدير PDF"
                        Style="{StaticResource ActionButton}"
                        Background="#E91E63"
                        Click="ExportPDF_Click">
                    <Button.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="5" ShadowDepth="2"/>
                    </Button.Effect>
                </Button>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" 
                Background="{DynamicResource CardBackground}" 
                Padding="20" 
                Margin="0,0,0,15"
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="{Binding IsAddingLine, Converter={StaticResource BoolToVis}}">
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="إضافة/تعديل خط" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                <TextBlock Text="ملاحظة: اضغط Enter بعد كل حقل للانتقال تلقائياً للحقل التالي (دعم QR Scanner)" 
                           FontSize="12" Foreground="#666" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                        <TextBlock Text="اسم الشخص المرتبط" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtName"
                                 Text="{Binding NewLine.Name, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 KeyDown="TextBox_KeyDown"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                        <TextBlock Text="الرقم القومي" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtNationalId"
                                 Text="{Binding NewLine.NationalId, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 MaxLength="14"
                                 KeyDown="TextBox_KeyDown"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                        <TextBlock Text="رقم الخط" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtPhoneNumber"
                                 Text="{Binding NewLine.PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 KeyDown="TextBox_KeyDown"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                        <TextBlock Text="ID داخلي" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtInternalId"
                                 Text="{Binding NewLine.InternalId, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 KeyDown="TextBox_KeyDown"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                        <CheckBox x:Name="chkHasCashWallet"
                                  Content="يملك محفظة كاش"
                                  IsChecked="{Binding NewLine.HasCashWallet}"
                                  FontWeight="Bold"
                                  Margin="0,10,0,5"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="5">
                        <TextBlock Text="رقم المحفظة (اختياري)" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtCashWalletNumber"
                                 Text="{Binding NewLine.CashWalletNumber, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 IsEnabled="{Binding NewLine.HasCashWallet}"
                                 KeyDown="TextBox_KeyDown"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="0" Margin="5">
                        <TextBlock Text="نظام الخط (اختياري)" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtLineSystem"
                                 Text="{Binding NewLine.LineSystem, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 KeyDown="TextBox_KeyDown"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="1" Margin="5">
                        <TextBlock Text="تفاصيل إضافية (اختياري)" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtDetails"
                                 Text="{Binding NewLine.Details, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10"
                                 FontSize="14"
                                 Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </Grid>

                <Border BorderBrush="#2196F3" BorderThickness="2" Background="#E3F2FD" Padding="10" Margin="5,10,5,5" Visibility="{Binding Group.IsBusinessGroup, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="مستوى التأكيد (Business Group)" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="✓" Width="50" Height="50" FontSize="24" Margin="5" Background="#4CAF50" Foreground="White" Click="BtnConfirm1_Click" ToolTip="تأكيد مستوى 1"/>
                            <Button Content="✓✓" Width="50" Height="50" FontSize="20" Margin="5" Background="#1E88E5" Foreground="White" Click="BtnConfirm2_Click" ToolTip="تأكيد مستوى 2"/>
                            <Button Content="✓✓✓" Width="50" Height="50" FontSize="18" Margin="5" Background="#9C27B0" Foreground="White" Click="BtnConfirm3_Click" ToolTip="تأكيد مستوى 3"/>
                            <Button Content="إلغاء التأكيد" Width="100" Height="50" FontSize="12" Margin="5" Background="#FF5722" Foreground="White" Click="BtnConfirmClear_Click"/>
                        </StackPanel>
                        <TextBlock Text="{Binding NewLine.ConfirmationLevel, StringFormat='المستوى الحالي: {0}'}" FontSize="12" Margin="0,5,0,0" Foreground="#666"/>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" Margin="5,15,5,5" HorizontalAlignment="Left">
                    <Button Content="حفظ الخط"
                            Style="{StaticResource ActionButton}"
                            Background="#4CAF50"
                            Command="{Binding SaveLineCommand}"/>
                    <Button Content="إلغاء"
                            Style="{StaticResource ActionButton}"
                            Background="#9E9E9E"
                            Command="{Binding CancelAddLineCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" 
                Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="15" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍 بحث:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                 Width="450"
                                 Padding="10"
                                 FontSize="14"
                                 VerticalAlignment="Center"
                                 ToolTip="ابحث عن الاسم، الرقم القومي، رقم الخط، ID، رقم المحفظة، نظام الخط، أو التفاصيل"/>
                        <TextBlock Text="{Binding Lines.Count, StringFormat='النتائج: {0}'}" 
                                   FontSize="12" 
                                   FontWeight="SemiBold"
                                   Foreground="#2196F3"
                                   VerticalAlignment="Center" 
                                   Margin="15,0,0,0"/>
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Lines}"
                      SelectedItem="{Binding SelectedLine}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      Background="Transparent"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource DataGridGridLines}"
                      RowHeight="60"
                      HeadersVisibility="Column"
                      CanUserResizeRows="False"
                      AlternatingRowBackground="{DynamicResource DataGridAlternateRow}"
                      IsReadOnly="True"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="اسم الشخص" Binding="{Binding Name}" Width="*" FontSize="14">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="الرقم القومي" Binding="{Binding NationalId}" Width="130" FontSize="13">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#546E7A"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="رقم الخط" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PhoneNumber}" 
                                           FontSize="13"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding Converter={StaticResource PhoneLineToColorConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="ID داخلي" Binding="{Binding InternalId}" Width="100" FontSize="13">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#757575"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="محفظة كاش" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding HasCashWallet, Mode=OneWay}" 
                                          IsEnabled="False" 
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="رقم المحفظة" Binding="{Binding CashWalletNumber}" Width="120" FontSize="13">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#00897B"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="نظام الخط" Binding="{Binding LineSystem}" Width="120" FontSize="13">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#1976D2"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="التفاصيل" Binding="{Binding Details}" Width="250" FontSize="12">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="Foreground" Value="#616161"/>
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="ToolTip" Value="{Binding Details}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="مستوى التأكيد" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="✓" FontSize="16" FontWeight="Bold" Foreground="{Binding ConfirmationLevel, Converter={StaticResource ConfirmLevelToColor}}" Margin="2" Visibility="{Binding ConfirmationLevel, Converter={StaticResource ConfirmLevel1Vis}}"/>
                                    <TextBlock Text="✓" FontSize="16" FontWeight="Bold" Foreground="{Binding ConfirmationLevel, Converter={StaticResource ConfirmLevelToColor}}" Margin="2" Visibility="{Binding ConfirmationLevel, Converter={StaticResource ConfirmLevel2Vis}}"/>
                                    <TextBlock Text="✓" FontSize="16" FontWeight="Bold" Foreground="{Binding ConfirmationLevel, Converter={StaticResource ConfirmLevelToColor}}" Margin="2" Visibility="{Binding ConfirmationLevel, Converter={StaticResource ConfirmLevel3Vis}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="تاريخ الإنشاء" Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" Width="140" FontSize="12">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#9E9E9E"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>