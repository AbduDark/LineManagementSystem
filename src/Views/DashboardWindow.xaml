
<Window x:Class="LineManagementSystem.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="📊 لوحة المعلومات" 
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft"
        Background="{DynamicResource WindowBackground}">

    <Window.Resources>
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="15" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource StatCard}" Margin="0,0,0,20">
            <DockPanel>
                <Button Content="🔙 رجوع" 
                        DockPanel.Dock="Left"
                        Click="BackButton_Click"
                        Padding="20,12"
                        Background="#607D8B"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="10"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📊 لوحة المعلومات والإحصائيات" 
                              FontSize="32" 
                              FontWeight="Bold"
                              Foreground="{DynamicResource TextPrimary}"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding LastUpdateTime, StringFormat='آخر تحديث: {0:dd/MM/yyyy hh:mm tt}'}" 
                              FontSize="14" 
                              Foreground="{DynamicResource TextSecondary}"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Groups -->
            <Border Grid.Column="0" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="📦" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding TotalGroups}" 
                              FontSize="48" 
                              FontWeight="Bold"
                              Foreground="#2196F3"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="إجمالي المجموعات" 
                              FontSize="18" 
                              Foreground="{DynamicResource TextSecondary}"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Total Lines -->
            <Border Grid.Column="1" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="📱" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding TotalLines}" 
                              FontSize="48" 
                              FontWeight="Bold"
                              Foreground="#4CAF50"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="إجمالي الخطوط" 
                              FontSize="18" 
                              Foreground="{DynamicResource TextSecondary}"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Active Groups -->
            <Border Grid.Column="2" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="✅" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding ActiveGroups}" 
                              FontSize="48" 
                              FontWeight="Bold"
                              Foreground="#8BC34A"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="المجموعات النشطة" 
                              FontSize="18" 
                              Foreground="{DynamicResource TextSecondary}"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Pending Alerts -->
            <Border Grid.Column="3" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="⚠️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding PendingAlerts}" 
                              FontSize="48" 
                              FontWeight="Bold"
                              Foreground="#FF9800"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="التنبيهات المعلقة" 
                              FontSize="18" 
                              Foreground="{DynamicResource TextSecondary}"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Charts -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Provider Distribution Pie Chart -->
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="📊 توزيع المجموعات حسب الشركة" 
                              FontSize="20" 
                              FontWeight="Bold"
                              Foreground="{DynamicResource TextPrimary}"
                              Margin="0,0,0,15"/>
                    <lvc:PieChart Series="{Binding ProviderSeries}" 
                                 LegendPosition="Bottom"
                                 Height="350"/>
                </StackPanel>
            </Border>

            <!-- Status Distribution Column Chart -->
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="📈 توزيع المجموعات حسب الحالة" 
                              FontSize="20" 
                              FontWeight="Bold"
                              Foreground="{DynamicResource TextPrimary}"
                              Margin="0,0,0,15"/>
                    <lvc:CartesianChart Series="{Binding StatusSeries}" 
                                       XAxes="{Binding XAxes}"
                                       Height="350"/>
                </StackPanel>
            </Border>

            <!-- Lines with Wallet Chart -->
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="💰 الخطوط بمحفظة نقدية" 
                              FontSize="20" 
                              FontWeight="Bold"
                              Foreground="{DynamicResource TextPrimary}"
                              Margin="0,0,0,15"/>
                    <lvc:PieChart Series="{Binding WalletSeries}" 
                                 LegendPosition="Bottom"
                                 Height="350"/>
                </StackPanel>
            </Border>

            <!-- Monthly Renewals Chart -->
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="📅 التجديدات الشهرية المتوقعة" 
                              FontSize="20" 
                              FontWeight="Bold"
                              Foreground="{DynamicResource TextPrimary}"
                              Margin="0,0,0,15"/>
                    <lvc:CartesianChart Series="{Binding RenewalSeries}" 
                                       XAxes="{Binding MonthXAxes}"
                                       Height="350"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
